import { EditPostDirective } from './edit-post.directive';

describe('EditPostDirective', () => {
  it('should create an instance', () => {
    const directive = new EditPostDirective();
    expect(directive).toBeTruthy();
  });
});
